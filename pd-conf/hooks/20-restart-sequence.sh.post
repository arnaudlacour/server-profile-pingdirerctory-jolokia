#!/usr/bin/env sh
# shellcheck disable=SC1090
. "${HOOKS_DIR}"/pingcommon.lib.sh
. "${HOOKS_DIR}"/nice.lib.sh

printf "Restoring original status CLI ... "
mv "${SERVER_BITS_DIR}"/bin/status.orig "${SERVER_BITS_DIR}"/bin/status
print_status ${?}
